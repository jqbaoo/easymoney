# Android 真机验收记录

| 项 | 值 |
|---|---|
| 验收日期 | 2026-09-13 |
| 设备型号 | Redmi K60 |
| Android 版本 | 14 |
| APK | `Builds/EasyMoney.apk`（29 MB / 29,843,835 字节） |
| 构建时间 | 2026-09-13 03:25 |
| 构建命令 | `bash Tools/build-android.sh` |
| 包名 | `com.easymoney.app` |
| 构建类型 | IL2CPP，Release，debug 签名 |

⚠️ **上表里的「Android 版本 14」与后来导航栏那几轮的说法对不上。** 那些轮的读数
（`safeArea` 底部内缩 0、强制 edge-to-edge 才解释得通）指向 **Android 15**，
SPEC 第 11 节也是按 15 写的。**两者以哪个为准没有核实过**——记在这里，别再往下传。

---

## ⚠️ 验收方式与可信度

**本次验收由用户在真机上手动执行，AI 未直接观察界面、未采集 logcat、未截图。**

Task 17 Step 5 原定用 `adb install` 装包、截图逐条核对。实际**未能这样做**，因为
测试机与开发机之间始终无法建立 adb 连接（原因见下方「验收环境问题」）。最终改用
「把 APK 传到手机、用户手动安装并操作」的方式完成验收。

因此这份记录的**可信度是有限的**，读它的人需要知道：

- 下表的「通过」**依据用户口述反馈**，不是 AI 的直接观察
- **没有 logcat 佐证**。真机上若发生过闪退或异常，本记录无法反映
- 「持久化」一项用户确认通过，但**不是用 `adb shell am force-stop` 精确杀死进程**验证的，
  而是「从最近任务里划掉」——两者在 Android 上不等价（划掉不保证杀死进程）
- 所有需要目测的项（中文显示、金额数字、占比百分比）都只有用户一个人的判断

---

## 结果

| 分组 | 通过 | 未通过 | 备注 |
|---|---|---|---|
| 基础可用性 | 3 | 0 | |
| 记账闭环 | 5 | 0 | |
| 转账 | 4 | 0 | |
| 持久化 | 1 | 0 | 见上方可信度说明 |
| 边界情况 | 4 | 1 | emoji 备注显示为空白——已定性为字体渲染，**数据没丢**，见下 |
| **合计** | **17** | **1** | |

### 逐条明细

**① 基础可用性**

| # | 项 | 结果 |
|---|---|---|
| 1 | 应用能启动，不闪退 | ✅ |
| 2 | 中文显示正常，无方块、无乱码 | ✅ |
| 3 | 底部四个标签能正常切换、不卡 | ✅ |

**② 记账闭环**

| # | 项 | 结果 |
|---|---|---|
| 4 | 新建账户「现金」初始余额 1000 | ✅ |
| 5 | 记一笔支出 35.50 / 餐饮 / 午饭，提示「已保存」 | ✅ |
| 6 | 账户页「现金」余额 964.50 | ✅ |
| 7 | 账单页本月可见该笔，支出 35.50、结余 -35.50 | ✅ |
| 8 | 报表页支出构成「餐饮」占 100% | ✅ |

**③ 转账**

| # | 项 | 结果 |
|---|---|---|
| 9 | 新建账户「支付宝」初始余额 0 | ✅ |
| 10 | 转账 200 从「现金」到「支付宝」 | ✅ |
| 11 | 现金 764.50、支付宝 200.00、总资产 964.50 不变 | ✅ |
| 12 | 转账出现在账单列表，但不计入报表收支 | ✅ |

**④ 持久化**

| # | 项 | 结果 |
|---|---|---|
| 13 | 从最近任务划掉后重开，数据全部还在 | ✅ |

**⑤ 边界情况**

| # | 项 | 结果 |
|---|---|---|
| 14 | 金额 0 → 提示「金额必须大于 0」 | ✅ |
| 15 | 金额 abc → 提示「请输入正确的金额，例如 12.50」 | ✅ |
| 16 | 中文备注（含 emoji）能正确保存和显示 | ⚠️ **部分通过** |
| 17 | 删除一笔记录，汇总与余额同步回退 | ✅ |
| 18 | 连续快速记账 20 笔，不卡顿、不崩溃 | ✅ |

---

## 未通过项详情

### #16 emoji 备注显示为空白 —— **已定性：字体渲染，数据没丢**

**操作步骤**：记账页填写备注，其中包含 emoji（如 `午饭🍜`），保存后查看。

**实际现象**：备注中的 emoji 显示为空白，填几个都是空。用户反馈「问题不大」。

**结论：(a) 字体画不出来，不是数据丢失。** 判定办法是把写入链路的**每一段**都证干净，
只剩渲染这一环：

| 环节 | 证据 | 结果 |
|---|---|---|
| 仓储往返 | `TransactionRepositoryTests.Note_SupportsChineseAndEmoji`（`和朋友吃饭🍜` 原样读回） | ✅ 干净 |
| 服务层（写账单必经之路） | `TransactionServiceTests.Save_KeepsEmojiNoteIntact`（本次新增） | ✅ 干净 |
| 展示投影 | `StatementBuilderTests.BuildRow_EmojiNote_KeepsItWholeAsTitle`（本次新增） | ✅ 干净 |
| 字体渲染 | `FontProvider` 候选全是中文字体，**都不含 emoji 字形**；legacy `Text` 字形缺失时不跨字体回退 | ❌ **就是这里** |

**为什么可以断定是字体**：`SqliteTransactionRepository` 的读写直接透传 `Note` 字段，
全项目没有任何 `Substring` / `Remove` / 按 `char` 截断的代码；上游两段又都有测试钉着。
字符串既然完整地交到了界面手上，画不出来就只能是字形缺失。

**两条新增测试做过反向验证**（按项目惯例，逐个核对失败用例名而非只看个数）：

- 注入「按 `char` 截断、劈开代理对」→ 挂 5 个，全在 `StatementBuilderTests`，
  且每一条都确认过是断言 `Title` 时才挂的（`"八月工资"→"八月工"`、`"午饭"→"午"`）
- 注入「写入时丢掉 emoji」→ **恰好挂 2 个**：`Note_SupportsChineseAndEmoji` 与
  `Save_KeepsEmojiNoteIntact`，没有多余失败

**影响**：观感问题。emoji 那部分不显示，同一条备注里的中文正常，**数据是完整的**。

**要支持 emoji 的话**：得自带含 emoji 字形的字体；但 Unity 的 legacy `Text` 不支持
彩色 emoji 回退，彻底解决需换 TextMeshPro。**当前判断为不值得做**——为一个备注里的
表情符号引入 TMP 迁移，对记账 App 不划算。

**与已知风险的关系**：SPEC 第 11 节已记录「中文字体」风险（`FontProvider` 的候选
一个都不命中会显示方块字，正式发布建议自带 `Fonts/main.ttf`）。emoji 是同一类问题的
另一个表现——**字体覆盖不足**。

---

## 验收环境问题（这些是下次要解决的）

本次验收没能走成 `adb install`，卡在连接上。两个坑都记下来，避免下次重踩。

### 1. USB 连接：Windows 侧完全识别不到设备

`adb devices` 始终为空，Windows 的 USB 设备列表里**连一个「未知设备」都没有**。

- 手机端能弹出「USB 用途」选择框（选了「传输文件」）
- Windows「此电脑」里看不到手机
- 结论：**数据线只有电源线芯**（能充电、能触发 USB 用途弹窗，但传不了数据）。
  不是驱动问题——驱动缺失会显示成带感叹号的未知设备
- **解决**：换一根确定能传数据的线

### 2. 无线调试：两个坑，第一个已解决

**(a) `adb mdns daemon unavailable`** —— 已解决 ✅

Unity 自带的 platform-tools（32.0.0）和 Google 官方 35.0.0 都报这个错。
根因是 **platform-tools v31.0.2+ 默认关闭了 mDNS 的 OpenScreen 后端**，需要显式开启：

```bash
export ADB_MDNS_OPENSCREEN=1
adb kill-server && adb start-server
adb mdns check       # → mdns daemon version [Openscreen discovery 0.0.0]
```

在此之前 `adb pair` 一律失败并报 `Failed: Unable to start pairing client.`

**(b) 路由器 AP 隔离** —— 未解决 ❌

mDNS 修好后，`adb pair` 仍连不上，因为网络层就不通：

- 电脑 `192.168.1.2`（有线），网关 `192.168.1.1`
- 手机 `192.168.1.3`（无线），网关 `192.168.1.1` —— **同一台路由器**
- 但 `ping 192.168.1.3` 返回「无法访问目标主机」，ARP 表里也没有该条目

同网段、同网关却二层不通，典型原因是**路由器开了 AP 隔离 / 客户端隔离**。
下次在路由器管理页关掉即可。

---

## 偏离计划的地方

Task 17 的计划文档（`2026-09-11-账单管理MVP.md` 第 7023 行起）有三处与实际不符，
执行时按实际情况处理了：

### 1. Target Architectures：保持 ARMv7 + ARM64，没照计划只勾 ARM64

计划 Step 2 的表格写「Target Architectures：只勾 ARM64」。**没有照做**，保持
`AndroidTargetArchitectures: 3`（ARMv7 | ARM64）。

理由：UNIT 已实测 ARMv7 的 SQLite 原生库在包内（见下方 APK 校验），只勾 ARM64 会
白白丢掉仍在使用 32 位设备的用户；纯 64 位也不是 Google Play 的要求（要求是**必须
提供** 64 位，不是只能有 64 位）。SPEC 第 11 节记录了这个决策，`AndroidBuildConfigTests`
和新增的 `AndroidPlayerSettingsTests` 一起守着它。

**APK 实测佐证**：

```
lib/arm64-v8a/libe_sqlite3.so     1,772,160 字节
lib/armeabi-v7a/libe_sqlite3.so   1,246,788 字节
```

### 2. Managed Stripping Level：实测默认已是 Minimal，未改配置

计划 Step 2 强调「Managed Stripping Level 必须设成 Minimal」，否则会剥离 sqlite-net
的反射代码、表现为真机闪退。

**实测结论：不用改。** `managedStrippingLevel` 字典里 Android 缺条目时，
`PlayerSettings.GetManagedStrippingLevel(BuildTargetGroup.Android)` 返回的就是
`Minimal`。反向验证时注入 `Android: 1` 才变成 `Low`——证实 `1 == Low`。

因此**没有手写 YAML 去「显式设置」**：asset 里其他平台存的值就是 `1`，手写枚举映射
有写错的风险，反而可能把 Minimal 写成别的值。改由 `AndroidPlayerSettingsTests`
的 `ManagedStrippingLevel_IsMinimal` 守卫。

### 3. Internet Access：设了 Not Required，但 APK 里仍有 INTERNET 权限

计划 Step 2 要求 `Internet Access: Not Required`。已设置，`ForceInternetPermission: 0`，
守卫测试 `InternetAccess_IsNotRequired` 也是绿的。

**但 APK 里依然存在该权限**：

```
$ aapt dump badging Builds/EasyMoney.apk
uses-permission: name='android.permission.INTERNET'
```

原因：`Packages/manifest.json` 里的 **`com.unity.modules.unitywebrequest`** 模块
自身会声明 INTERNET 权限，manifest merger 会把它合并进来——`ForceInternetPermission`
拦不住它。Unity 自带的 `LauncherManifest.xml` 模板里并没有这条权限。

对一个「单机无网络」的记账 App 来说这是个瑕疵（应用商店会显示「需要网络权限」），
但**不影响任何功能**，验收清单里也没有相关项。

**要真正去掉**，需要在 `Assets/Plugins/Android/AndroidManifest.xml` 写：

```xml
<manifest xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove"/>
</manifest>
```

自定义 manifest 是构建失败的高发区，**建议单独一轮做**，不塞进本次验收。

---

## APK 校验（构建后实测，这部分是 AI 直接观测的）

```bash
$ aapt dump badging Builds/EasyMoney.apk
package: name='com.easymoney.app' versionCode='1' versionName='1.0'
sdkVersion:'24'
targetSdkVersion:'35'
application-label:'EasyMoney'
application: label='EasyMoney' icon='res/W5.png'
application-icon-120/160/240/320/480/640  （6 档齐全）
native-code: 'arm64-v8a' 'armeabi-v7a'
```

| 项 | 结果 |
|---|---|
| 包名 `com.easymoney.app` | ✅ |
| MinSdk 24（计划值） | ✅ |
| TargetSdk 35（Automatic） | ✅ |
| 两个 ABI 都在 | ✅ |
| SQLite 原生库两个 ABI 齐全 | ✅ **最高风险点确认解除** |
| 应用图标（紫色钱包，6 档） | ✅ |
| Resources 里不再打包 AppIcon 源图 | ✅ |

---

## 后续待办

1. **真机复验第五版 APK。** ✅ 包已经打出来了（`Builds/EasyMoney5.apk`，
   35,152,123 字节 ≈ 33.5 MB，构建时间 2026-09-14 22:44）。
   第四版（`Builds/EasyMoney4.apk`）已验过一轮：第 29、30 项过（导航键常驻、
   手势下标签栏贴底），**第 32 项不过**（三键下位置偏高）——第五版修的正是它。
   清单见下面「真机待验收」一节，重点看**第 32-34 项**
   - 打包后核对过清单：`unity.launch-fullscreen` = **false**、
     `unity.render-outside-safearea` = **true**、`screenOrientation` = 1（竖屏）、
     minSdk 24 / targetSdk 35、arm64-v8a + armeabi-v7a 两个 ABI 都在
2. **解决连接问题**，下次验收才能真正用上 `adb install` + logcat + 截图
   - 换一根 USB 数据线（首选）
   - 或关掉路由器 AP 隔离走无线调试
3. 决定要不要去掉 INTERNET 权限（自定义 AndroidManifest）
4. emoji 若要支持，需自带含 emoji 字形的字体 + 换 TextMeshPro。**当前判断为不值得做**

已了结：

- ~~确认 #16 是 (a) 还是 (b)~~ —— 已定性为 (a) 字体渲染，数据没丢，用测试定的（见上）

---

## 真机待验收：底部导航栏遮挡修复（2026-09-13）

⚠️ **这一节已经走过四轮真机了，剩下的还是待办**——每轮修好一个、又暴露下一个，
第 19-28 项要在**第五版** APK 上再走一遍，另加第 29-33 项。四轮各自的结论见下面四个小节。

为什么要单独列一节：这次修的是「三键导航时整块底部被盖住」，而**编辑器里完全看不到
这个现象**——`Screen.safeArea` 在全屏、导航栏高度恒 0，改动在 Play 模式下不可见。
所以 Play 最多只能证明「没有回归」，证明不了「修好了」，与前面几轮的性质不同。

编号接上面真机验收的 1-18 往下数。

| # | 检查项 | 期望 / 判据 |
|---|---|---|
| 19 | 切到**三键导航**，标签栏四个标签完整可见 | 不被系统导航栏盖住 |
| 20 | 账户页「添加账户」按钮完整可见、点得到 | 这是原先被盖得最狠的一处 |
| 21 | 报表页明细列表滚到底，最后一行不被遮 | |
| 22 | 记一笔页、账单页底部同样正常 | 四个页面都要看，别只看用户报的两个 |
| 23 | 底部导航栏那一条**不是蓝灰色**的 | 应该是页面底色（燕麦米白）。蓝灰 = `Background` 还挂在 `SafeArea` 里，说明改动没生效 |
| 24 | 切到手势导航，再切回三键，布局自动跟随 | `OnApplicationFocus` 那条路径。**不跟随 = 导航栏高度被缓存成旧值** |
| 25 | 找一台 Android 13/14 的机器看**有没有多出一条白边** | 幂等规则生效的证据。手边没有这类机器就跳过并注明 |
| 26 | 抄下屏幕上那行调试读数 | safeArea 的 y/h/w、屏幕尺寸与 `gap`、两个 navBar 取值、`vis`、`tap`、`inset`、算出的 extra |
| 26b | **底部那 124px 里看得到返回 / 主页 / 多任务三个键** | 这三个键是深色的。看不见 = 图标颜色没生效，见下面第三轮那张判读表 |
| 27 | 字重层次（页标题 / 金额 / 正文）分得开，中文不发糊 | 前六次没进包的改动之一 |
| 28 | 暖色配色、卡片投影、月份条选月、环形图与图例色点 | 前六次没进包的其余改动，在真机上从没见过 |

**第 26 项的值请抄回来**——它是唯一能区分「JNI 没读到时序」和「这台设备本来就不需要补」
的东西，而这两种情况在界面上长得一模一样。读数用的是临时组件 `SafeAreaDiagnostics`
（只在 Android 上编译），**验完连同它一起删掉**。

跑完这一轮之后，第 23 / 24 / 25 项是这次修复的直接证据，其余是顺带走查。

### 第一轮（构建 1）：没修好 —— 但读数把原因指出来了

**结果：第 19-22 项不过**（用户肉眼：「三键导航的情况下底部标签还是被覆盖了」），
**第 23 项过了**（用户肉眼：「导航栏是米白色」，不是蓝灰——说明 `Background` 挪出
`SafeArea` 那一步已经生效）。

抄回来的读数（`Assets/结果图.jpg`，1080×2400 的截图）：

```
safeArea y=0 h=2310 w=1080
screen 1080x2400  nav 0 (new 0/old 124)  extra 0
```

**三件事一次问清了**，这正是当初坚持要塞这个临时读数的理由：

| 读数 | 说明了什么 |
|---|---|
| `safeArea y=0 h=2310`，屏幕高 2400 | 底部**贴着屏幕最底**（`y=0`），排掉的那 90px 全在**顶部**（状态栏/挖孔）。UUM-121413 在真机上坐实：`safeArea` 确实不含导航栏 |
| `new 0` | API 30+ 的 `getInsets(Type.navigationBars())` 在这台机器上返回 **0**——正路是哑的 |
| `old 124` | deprecated 的 `getSystemWindowInsetBottom()` 给出了正确的 124px |

于是 `nav` 取到 0、`extra` 算出 0，**修了跟没修一样**——而界面上完全看不出原因，
只有读数能区分「JNI 读不到」和「设备不需要补」。

**修法**：新增 `SafeAreaLayout.ResolveNavigationBarHeight(iNewApiPx, iLegacyPx)`——
新 API 优先，**它为 0 才回退老的**，两个都没有就是 0。刻意**不取两者较大值**：
两个来源都可能给出与当前导航模式不符的偏大值（手势导航下导航栏只有一条细缝），
取大就会多让出一截白边，同样不报错。纯函数，5 条用例钉着（`SafeAreaLayoutTests`）。

⚠️ **另一个独立的问题**：改完之后**打包失败**，报
`AndroidSystemBars.cs(133,20): error CS0103: 'SafeAreaLayout' does not exist`——
漏了 `using EasyMoney.Core;`。**364 个 EditMode 用例当时全绿**，因为出错那句在
`#if UNITY_ANDROID && !UNITY_EDITOR` 块里，编辑器根本不编译它。
详见 `Assets/Scripts/App/CLAUDE.md` 里那条「别用 `#if UNITY_ANDROID && !UNITY_EDITOR`
把代码藏起来」。

### 第二轮（构建 2）：位置对了 —— 但底部空出一块

提交 `f0113e57`。**第 19-24 项过了**：内容不再被盖，读数也从 `nav 0` 变成了
**`nav 124 (new 0/old 124) extra 124`**——回退取法接上了，用户原话「位置是对的」。

**但冒出来一个新毛病**：底部空出 124px 什么都没有（用户原话「导航键锁起来的时候好奇怪，
下面空着」）。截图 `Assets/结果图2.jpg`。

**读数把范围收窄了**：`nav` 报的是 **124**，这是每次刷新实时读的、不是缓存值——
说明系统**此刻**认为导航栏正占着那 124px。真被藏起来的话它会变成 0，那 124px 会被内容
吃掉、标签栏直接贴到屏幕最底；而截图里标签栏正好停在它上面。**占着地方却看不见东西，
那就只剩一种可能：画了，但颜色和底一样。**

去 APK 里查证，根因在 Unity 生成的主题
（`Library/Bee/Android/Prj/IL2CPP/Gradle/unityLibrary/src/main/res/values-v31/styles.xml`）：

```xml
<style name="BaseUnityTheme" parent="android:Theme.Holo.Light.NoActionBar.Fullscreen">
```

**Holo 是 API 27 之前的东西，压根没有 `windowLightNavigationBar` 属性**，取默认值
`false` = 让系统画**白色图标**。Android 15 之前一直没暴露——那时导航栏是不透明的黑条，
白图标配黑底正好；是 edge-to-edge 把导航栏变成没有底色的浮层之后，图标颜色才开始要紧。

### 第三轮（构建 3）：带上了导航栏外观修正

提交 `c74d0d95`。启动时经 `WindowInsetsController` 设深色图标 + `show()` +
`BEHAVIOR_DEFAULT`（只在 API 30+）。

**下面 10 项要在这一版上走第三遍**（第 23 项已知过，可只扫一眼）。⚠️ **第 26 项现在多了一位
`vis`**——光看 `nav` 分不出「系统把导航栏藏了」和「画了但看不见」，这两种要改的代码完全不同。

| 读数 | 判读 |
|---|---|
| `vis 1` + 底部看得到返回/主页/多任务三个键 | ✅ 修好了 |
| `vis 0` | 系统确实把导航栏藏了，`show()` 没顶住，是另一条路子 |
| `vis 1` 但还是看不见 | 图标颜色没生效，回头查 `setSystemBarsAppearance` 那条 JNI 有没有抛 |

### 第四轮（构建 4）：导航键要常驻，手势下标签栏要落到底

第三轮的实机结果（用户肉眼）是两句话：

- **图标改深色那步成功了**——三个键看得见了，但**会自己慢慢消失**，
  消失后往上划才浮出来、过一两秒又缩回去
- 手势导航下**标签栏还是悬着，底下空一条**

这两条是**两个独立的根因**，这一版一起收：

**① 「会自己消失」不是没让它显示，是我们自己请求了全屏沉浸。**
Player Settings 里的「Start in Fullscreen Mode」会往清单里写
`unity.launch-fullscreen=True`，播放器据此给窗口设
`SYSTEM_UI_FLAG_HIDE_NAVIGATION | SYSTEM_UI_FLAG_IMMERSIVE_STICKY`——
这两个字符串在 `libunity.so` 里直接搜得到。现象与 `IMMERSIVE_STICKY` 的定义一字不差：
**划一下浮出来、一两秒又缩回去**。微信这类应用不请求全屏，导航键才是常驻的。
第三轮只在 `AppRoot.Awake` 里调了一次 `show()`，**时机在播放器设标志之前，被它盖掉了**。
两处一起改：关掉 `androidStartInFullscreen`（掐根因），并把
`EnsureNavigationBarUsable()` 挪进 `SafeAreaFitter._apply()`——每次重算安全区重申一遍，
正好覆盖启动头两秒（重试窗口）与切回前台这两个时间点。

**② 「手势导航下空一条」是拿导航栏高度当预留高度用了。**
某些机型在手势导航下照样报出三键的高度，光看 inset 分不出导航模式。
新增 `SafeAreaLayout.ResolveBottomInset`，用 `WindowInsets.Type.tappableElement()`
分辨——**它为 0 就是手势导航**（没有可点的系统栏），非 0 是三键；这是 Android 官方
edge-to-edge 指引给的分辨办法。手势下预留 **0**（标签栏落到底，微信的样子），
三键下留整条。读不到（API 30 以下）时按「有导航键」兜底：多留一截只是难看，
少留一截是内容被系统栏压住、再也点不到。

**第 26 项的读数现在多一位 `tap`**（系统栏可点区域高度）：

| 读数 | 判读 |
|---|---|
| `tap 124` / `tap 0` | 三键 / 手势，与看到的导航键对得上就说明分辨是对的 |
| `tap -1` | 读不到，代码按三键兜底——此时底部会留一条，是预期行为 |

**这一轮要在新版上重走第 19-28 项**（第 26 项连 `tap` 一起抄），另加三条：

| # | 检查项 | 期望 / 判据 |
|---|---|---|
| 29 | 三键导航下**放着别碰**，看导航键会不会自己消失 | 一直在、不用往上划。**这是本轮的主诉** |
| 30 | 切到手势导航，标签栏**贴着屏幕最底** | 底下不再空一条。手势提示条压在标签栏底边上是对的 |
| 31 | 三键 ↔ 手势来回切，两个方向都对 | 三键：让出整条导航栏；手势：贴底 |

### 第五轮（构建 5）：窗口已经让开底部时不再重复预留

第四轮的实机结果（用户肉眼）是两句话：

- ✅ **手势导航下正常了**（第 30 项过）；✅ **导航键不再自己消失**（第 29 项过）
- ❌ 三键导航下**位置偏高了**，而且**导航栏那条是黑的**（第 32、33 项）
- ❓ 用户同时问「为什么背景是黑色的」——见下面第 33 项，那是两处黑，来源不同

抄回来的读数（1080×2400 的截图）：

```
screen 1080x2276  nav 124 (new 124/old 124)  vis 1  tap 124  extra 124
```

**`screen 1080x2276` 就是答案的一半**：2400 的屏少掉 **124**，正好一个导航栏——
窗口自己已经停在导航栏上沿了。而 `nav` **照样报 124**，于是 `extra` 又让了一次，
标签栏凭空抬高一条。**光看 inset 分不出「窗口铺在导航栏下面」和「窗口停在导航栏
上沿」**，这两种要的答案恰好相反，而界面上都不会报错。

⚠️ 顺带记一笔：第一轮读数里 `new 0`（新 API 是哑的），这一轮变成了 `new 124`。
差别是关掉「Start in Fullscreen Mode」——**窗口不再是全屏沉浸态之后，inset 才正常
分发下来**。所以第 19-28 项里那条「新 API 优先、为 0 才回退老的」现在走的是新 API
那条路，回退仍在（别的机型仍可能是哑的），但**这台机器上已经验不到回退分支了**。

**修法**：`ResolveBottomInset` 加第三个判据 `iWindowBottomGapPx`（渲染面底边到屏幕
底边的距离，读 `Display.main.systemHeight` 与 `Screen.height` 的差），
**间距够一整条就让 0**。

⚠️ **判据是「够不够一整条」，不是把差额减掉。** 减差额（124-60=64）在「顶部让出
状态栏、底部却铺到导航栏下面」的机器上会少留一截，方向恰好反了。真机上窗口只会停在
两个位置：贴着屏幕底，或者停在导航栏上沿。

**两处「黑色」是两个来源**，别当成一个：

| 位置 | 是什么 | 处置 |
|---|---|---|
| 顶部那条深色横带 | **临时诊断面板自己**（`Theme.SCRIM` 半透明黑底 + 白字），而且它的第一行被状态栏盖住了 | 面板下移 `PANEL_TOP_OFFSET = 140` 并加高，验完整体删掉 |
| 底部导航栏那条 | 系统画的导航栏**自己的底色**——窗口已经不在那儿了，应用画不到 | 试 `Window.setNavigationBarColor(Theme.BACKGROUND)`。**Android 15 上系统可能忽略**，忽略了就退回现状，不会更糟 |

**这一轮要在新版上重走第 19-28 项**（第 26 项的读数现在多一位 `inset`，见下），
另加三条：

| # | 检查项 | 期望 / 判据 |
|---|---|---|
| 32 | 三键导航下**标签栏正好落在导航键上方**，底下不再空一条 | **这是本轮的主诉**。即 `gap 124` → `inset 0`、`extra 0` |
| 33 | 底部导航栏那一条是**页面底色**（燕麦米白）而不是黑的 | 系统忽略了 `setNavigationBarColor` 就还是黑的，**记下来即可，不是回归** |
| 34 | 手势导航下**没有被这轮改回去**（标签栏仍贴底） | 上一轮刚修好的，别为了修三键把它弄回去 |

**第 26 项现在多一位 `inset`**——`nav` 与 `inset` 是**两个数**，别混着看：
`nav` 是「导航栏报多高」，`inset` 是「最终决定让多少」。三键 + 窗口铺满时两者相等，
只要窗口自己让开了底部，就是 `nav 124` 配 `inset 0`：

| 读数 | 判读 |
|---|---|
| `gap 124` + `nav 124` + `inset 0` | ✅ 本轮修好的形状（三键） |
| `gap 0` + `nav 124` + `inset 124` | 窗口仍铺到屏幕底，该留就得留——**这不算错**，看界面对不对（老 Android 上会是这样） |
| `gap 124` 但 `inset 124` | 改动没生效，回头查 `_windowBottomGapPx()` 那两个来源 |
| `tap 0` + `inset 0` | 手势导航，与上一轮一致 |

---

## 编辑器内 Play 验收记录

**这里是 Unity 编辑器点 Play 的肉眼验收，不是真机验收**——两者不要混。
本文件上半部分是真机（Redmi K60）的结果，真机验收一律装全新的。

### 2026-09-13：配色与投影 + 月份条选月

上一版 APK 之后又改了两次纯视觉的东西，EditMode 都测不到，所以单独走了一遍 Play。

| # | 验收项 | 结果 |
|---|---|---|
| 1 | 暖色纸感配色与卡片投影（底色/卡片层次分得开） | ✅ |
| 2 | 选月弹窗：12 格摆成 3 列 4 行，无溢出、无挤压 | ✅ |
| 3 | 月份条：加了箭头后整组仍然居中 | ✅ |
| 4 | 年月文字右边的下拉提示**不是空白、也不是方块**（`↓` 在字体子集里） | ✅ |
| 5 | 遮罩盖住整个内容区、面板居中 | ✅ |
| 6 | 跨年：1 月点「Prev」→ 上年 12 月；12 月点「Next」→ 次年 1 月 | ✅ |

**6 项全过，无遗留问题。** 对应提交 `5efbaf7d`（配色）、`48ac2d7a`（月份条选月）。

⚠️ 这两次改动**当时都没打新 APK**，真机复验待做。这一条从那以后一直没销掉——
最多时累计有六次改动没进包。**已经销掉：第四版 APK（`Builds/EasyMoney4.apk`）
把这六次连同导航栏那三轮修复一次补齐了**，见「后续待办」第 1 条。

### 2026-09-13：报表环形图 + 视图下拉

报表新增了环形图视图，顶上加了视图下拉。这一版的界面部分同样一条 EditMode 都够不着，
其中**下拉面板的位置尤其测不了**（布局组算出的坐标要在真实布局重建之后才有，
`DropdownButton` 的定位注释里写明了这条）。

| # | 验收项 | 要点 | 结果 |
|---|---|---|---|
| 1 | 报表页筛选行右侧出现「条形图 ↓」下拉，与支出/收入两个按钮**不挤、不重叠** | 下拉定宽 220，两个 Tab 是 `SetFlexible` | ✅ |
| 2 | 点下拉：面板弹在**按钮正下方**、右边缘对齐 | 定位走 `CalculateRelativeRectTransformBounds`，换分辨率才不歪 | ✅ |
| 3 | 面板往**左下方**展开，左边不出屏 | 调用方（筛选行最右侧）得留出地方 | ✅ |
| 4 | 遮罩**全透明**（不压暗），点面板以外任意处收起 | 下拉是切换语义，刻意不做成模态 | ✅ |
| 5 | 当前项用主色 + 更重字重标出，一眼能看出选的是哪个 | 没有打勾图标资源 | ✅ |
| 6 | 切到环形图：环画出来了、**没有裂缝**、扇区颜色相邻可分 | 末片收口只补 ≤1 度的缝 | ✅ |
| 7 | 环心合计金额与明细行金额加起来**对得上** | 合计走 `ReportForm.BreakdownTotal`，从各项自己加 | ✅ |
| 8 | 环边缘**够不够清晰** | `DonutSprite.TEXTURE_SIZE = 320` 这个数就是在这一步定的，糊了就往大调 | ✅ |
| 9 | 环形图的明细行**不带条形**，行高比条形图那版**矮一截**（88 vs 112） | 行里只剩一行文字时按 112 排会显得很散 | ✅ |
| 10 | 切视图后**旧的节点没留在下面**（没有两层叠着画） | 这是最要紧的一条，EditMode 里验的是清场逻辑，肉眼要确认渲染结果 | ✅ |
| 11 | 选中的还是当前那项时，面板收起、页面**不重刷**（不闪） | `_pick` 里那条提前 return | ✅ |
| 12 | 空态：某类型一条记录都没有时，两种视图都只显示同一句提示 | 不画空环 | ✅ |
| 13 | 环形图配色与暖色纸感底色**不打架**（尤其焦糖棕 `#A9714B` 与色板里的棕） | 色板 8 色见 `ThemePalette.ChartColors` | ✅ |
| 14 | 切月份 / 切收支后环形图跟着重画，**贴图没有泄漏**（长时间反复切不掉帧） | `DonutSprite` 重画前先释放上一张 | ✅ |

**14 项全过。** 下拉面板的位置、环形图观感、切视图清场这些测不到的东西，这次都确认了。

### 2026-09-13：图例色点 + 收入 tab 字色 + 记账页保留选项

紧接着上一节的三处修正，其中第 1 条是用户肉眼发现的 bug（测试当时是绿的）。

| # | 验收项 | 要点 | 结果 |
|---|---|---|---|
| 15 | 报表页「**收入构成**」按钮：未选中时字是**深色**、看得见；选中时是主色底 + 白字 | 原来未选中是暖白底配白字 = 白底白字。测试能钉住配色规则，但「看上去对不对」还得看一眼 | ✅ |
| 16 | 「支出构成」按钮同样两种情况都正常 | 两处互为反面，别只验一个 | ✅ |
| 17 | 记一笔页的「支出/收入/转账」三个分段：字色同样正常 | 这段是**写对的那一份**，顺带回归一下没收坏 | ✅ |
| 18 | 环形图明细行**最左有一个彩色小圆点**，颜色与环上对应扇区**一致** | 这是本轮的重点。色点该是**正圆**不是椭圆（行高比它大，靠 `preserveAspect` 撑住） | ✅ |
| 19 | 色点排在**分类图标之前**，扫一列下来像一张图例；不与图标挤在一起 | 色点 20、图标 40、间距 12，真机上确认不挤 | ✅ |
| 20 | 切到**条形图**：明细行**没有**色点 | 条形图的条用收支红绿，色点会跟它打架 | ✅ |
| 21 | 分类数**超过 8 个**（色板长度）时颜色回绕，第 9 个与第 1 个同色——能接受 | 已知取舍，两条同色会有点混，看实际观感要不要扩色板 | ✅ |
| 22 | 记账页保存一笔后：**金额与备注已清空**，而**类型 / 账户 / 分类 / 日期都还在** | 本轮改动里唯一没有测试覆盖的一条 | ✅ |
| 23 | 连续记第二笔时不用重选账户与分类，直接填金额即可 | 这才是这条改动的目的，一次保存验不出来 | ✅ |
| 24 | 保存后**快捷金额档位跟着更新**（刚记的这笔进了「最近消费均值」） | `_clearInputs` 里补的那次刷新 | ✅ |
| 25 | 切到别的页再切回记一笔：**填了一半的内容还在**（不再被冲掉） | `OnShow` 的首次判断 | ✅ |
| 26 | 新装的库（还没有账户）进记一笔 → 去账户页建一个 → 切回来，账户行**自动选上了** | `_applyDefaults` 只补「还没选」的那条路 | ✅ |
| 27 | 保存的提示「已保存」**没有被「清空输入」顺手抹掉** | 顺序 bug，很容易犯：`_clearInputs` 里有 `_clearMessage` | ✅ |

**上表 13 项 + 前一节的 14 项，共 27 项全过，无遗留问题。** 2026-09-13 由用户在
Unity 编辑器里点 Play 逐项走查。对应提交见 `feat: 报表新增环形图视图，用下拉浮层切换`
（前一节）与 `fix: 修报表页收入构成未选中时白底白字，并给环形图加图例色点`。

这一轮验的东西比前几次都杂，值得记下两条：

- **第 15 条那个 bug 是用户肉眼发现的，当时 335 个用例全绿。** 配色规则、控件被画成
  什么颜色，测试都能钉住；但「这两个颜色配在一起看不看得见」钉不住——所以
  `TogglePaletteTests` 里那两条才改用 WCAG 对比度而不是「等于某个常量」。
  这一条本次已验过：收入构成未选中时字是深色的，看得见
- **第 22-27 条那一整块记账页行为，至今没有测试覆盖**，只能靠这次 Play 验。
  它验的是「保存后该留什么、不该留什么」，属于交互时序，不是能抽成纯函数的东西——
  `_onSave` 要连着 `AppContext` 和数据库才走得完。以后改这块要重新走一遍上表

⚠️ EditMode 那边 347 个用例全绿，但**测试全绿不代表界面验过**。这一版新增的界面部分里，
测试能给的只有「建了什么节点、切视图有没有清场、回调有没有触发、配色常量对不对」，
位置、观感、清晰度都在上表里。
